---
name: Renovate automerge

# yamllint disable-line rule:truthy
on:
  pull_request_target: {}

jobs:
  automerge:
    uses: timmo001/workflows/.github/workflows/renovate-automerge.yml@0bce6ebce1b88f020b1e356b526ba30af674c972 # master
    permissions:
      pull-requests: write
      contents: write
